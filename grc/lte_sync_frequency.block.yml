# auto-generated by grc.converter

id: lte_sync_frequency
label: CP FFO Calculator
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int
-   id: sig
    label: Signal Source
    dtype: raw

inputs:
-   domain: stream
    dtype: complex

templates:
    imports: import lte
    make: lte.sync_frequency(self.${sig}, ${fftl}, "$id")

file_format: 1
